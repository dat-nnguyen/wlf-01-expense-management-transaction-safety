# Wealify Guardian — Web UI

Next.js frontend application providing conversational chat, expense radar, transaction inspection cards, and reconciliation alerts.

## Getting Started

```bash
cd apps/web
npm install
npm run dev
```

Runs on [http://localhost:3000](http://localhost:3000).
